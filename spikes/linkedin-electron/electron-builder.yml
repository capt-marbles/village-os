appId: com.village.internal.linkedin-compatibility-spike
productName: Village LinkedIn Compatibility Spike
asar: true
files:
  - dist/**
  - package.json
mac:
  target: dir
  identity: "-"
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
artifactName: village-linkedin-compatibility-spike-${version}-${arch}.${ext}
