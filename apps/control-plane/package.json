{
  "name": "@village/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev --port 8787 --local",
    "db:migrate:local": "wrangler d1 migrations apply VILLAGE_DB --local",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@village/contracts": "workspace:*",
    "jose": "6.1.3",
    "zod": "4.4.3"
  }
}
