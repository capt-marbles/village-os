{
  "name": "@village/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@village/contracts": "workspace:*",
    "jose": "6.1.3",
    "zod": "4.4.3"
  }
}
